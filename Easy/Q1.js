// Return the Sum of Two Numbers

// Create a function that takes two numbers as arguments and returns their sum.

function sum(Num1,Num2){
    const add = Num1 + Num2;
    return add;
}

const execute = sum(20,30)
console.log(execute);
